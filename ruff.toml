line-length = 100
# Alembic writes these; don't enforce our import style on generated migrations.
extend-exclude = ["migrations/versions"]

[lint]
select = ["E", "F", "I"]

[lint.per-file-ignores]
"app/models/__init__.py" = ["F401"]
"app/main.py" = ["F401"]
