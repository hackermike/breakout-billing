__pycache__/
*.pyc
*.pyo
.Python
*.db
.env
.venv/
venv/
data/
dist/
build/
*.egg-info/

# Session-signing key (per install; never commit)
.secret_key

# Claude Code local permission grants (machine-specific, not for sharing)
.claude/settings.local.json

# Database backups (may contain patient data)
backups/

# Throwaway browser-driver / debug scripts (write with editor tools, run with node)
scripts/dev/
